{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"dfstore","text":"<p>dfstore is a lightweight, serverless DataFrame storage library. Save, version, and retrieve pandas and polars DataFrames from a local store \u2014 via Python, CLI, or a web UI.</p>"},{"location":"#why-dfstore","title":"Why dfstore?","text":"<p>Working with DataFrames across notebooks, scripts, and experiments often means wrestling with ad-hoc file naming (<code>data_v2_final_REAL.csv</code>) and no memory of what changed. dfstore solves this cleanly:</p> <ul> <li>Every <code>save()</code> creates a new version automatically \u2014 no naming required.</li> <li>Metadata (description, tags, shape, dtypes) is stored alongside the data.</li> <li>Load by name; optionally pin to a specific version.</li> <li>No database, no server \u2014 just plain Parquet files on disk.</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>import dfstore\nimport pandas as pd\n\ndf = pd.read_csv(\"sales.csv\")\n\n# Save\ndfstore.save(df, name=\"sales_2024\", description=\"Annual sales\", tags=[\"finance\"])\n\n# Load latest\ndf = dfstore.get(\"sales_2024\")\n\n# Load a specific version\ndf_v1 = dfstore.get(\"sales_2024\", version=1)\n</code></pre>"},{"location":"#at-a-glance","title":"At a Glance","text":"Feature Description Versioning Every save is a new version; old versions are always accessible Diff tracking Row count delta, added/removed columns recorded per version pandas &amp; polars Save either, load in whichever library you prefer No server needed Plain files on disk at <code>~/.dfstore</code> Soft delete Hide a DataFrame without destroying data; restore any time Web UI Built-in browser interface for browsing, uploading, and managing CLI Full command-line interface for shell scripting and pipelines"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install dfstore          # core library + CLI\npip install 'dfstore[gui]'   # + web UI (Flask)\n</code></pre>"},{"location":"cli/","title":"CLI Reference","text":"<p>dfstore ships with a full command-line interface powered by Typer.</p> <p>All commands accept <code>--store-path</code> to override the default store location.</p>"},{"location":"cli/#save","title":"save","text":"<p>Save a CSV or Parquet file to the store.</p> <pre><code>dfstore save &lt;file&gt; --name &lt;name&gt; [options]\n</code></pre> Option Description <code>--name</code>, <code>-n</code> DataFrame name (required) <code>--description</code>, <code>-d</code> Description <code>--tags</code>, <code>-t</code> Tag (repeat for multiple). Use <code>key=value</code> for dict tags <code>--notes</code> Version notes <code>--store-path</code> Override store directory <p>Examples:</p> <pre><code># Save a parquet file\ndfstore save data.parquet --name sales_2024 --description \"Annual sales\"\n\n# Save a CSV with tags\ndfstore save data.csv --name employees --tags hr --tags env=production\n\n# Read from stdin\ncat data.parquet | dfstore save - --name stream_data\n</code></pre>"},{"location":"cli/#get","title":"get","text":"<p>Retrieve a DataFrame and print it (or write to file).</p> <pre><code>dfstore get &lt;name&gt; [options]\n</code></pre> Option Description <code>--version</code>, <code>-v</code> Specific version number (default: latest) <code>--output</code>, <code>-o</code> Write to file instead of stdout <code>--format</code>, <code>-f</code> Output format: <code>csv</code> (default), <code>parquet</code>, <code>json</code> <code>--store-path</code> Override store directory <p>Examples:</p> <pre><code># Print as CSV\ndfstore get sales_2024\n\n# Get a specific version\ndfstore get sales_2024 --version 1\n\n# Save to file\ndfstore get sales_2024 --output out.parquet --format parquet\n\n# Pipe into another tool\ndfstore get sales_2024 --format json | jq '.[0]'\n</code></pre>"},{"location":"cli/#list","title":"list","text":"<p>List all stored DataFrames.</p> <pre><code>dfstore list [options]\n</code></pre> Option Description <code>--include-deleted</code> Include soft-deleted entries <code>--store-path</code> Override store directory <p>Example:</p> <pre><code>dfstore list\ndfstore list --include-deleted\n</code></pre>"},{"location":"cli/#info","title":"info","text":"<p>Show full metadata for a DataFrame.</p> <pre><code>dfstore info &lt;name&gt; [--store-path PATH]\n</code></pre> <p>Displays description, tags, timestamps, current version, shape, column dtypes, and null counts.</p>"},{"location":"cli/#search","title":"search","text":"<p>Search DataFrames by description, tags, or column names.</p> <pre><code>dfstore search [options]\n</code></pre> Option Description <code>--description</code>, <code>-d</code> Description substring <code>--tags</code>, <code>-t</code> Tag filter (repeat for multiple) <code>--columns</code>, <code>-c</code> Column name filter (repeat for multiple) <code>--store-path</code> Override store directory <p>Examples:</p> <pre><code># Search by description\ndfstore search --description \"sales\"\n\n# Search by tag\ndfstore search --tags finance --tags env=production\n\n# Search by column\ndfstore search --columns revenue --columns region\n</code></pre>"},{"location":"cli/#versions","title":"versions","text":"<p>Show the version history for a DataFrame.</p> <pre><code>dfstore versions &lt;name&gt; [--store-path PATH]\n</code></pre> <p>Displays version number, save timestamp, notes, shape, row diff, and columns added/removed.</p>"},{"location":"cli/#delete","title":"delete","text":"<p>Delete a DataFrame.</p> <pre><code>dfstore delete &lt;name&gt; [--hard] [--store-path PATH]\n</code></pre> <p>Without <code>--hard</code>, performs a soft delete (hidden but recoverable). With <code>--hard</code>, asks for confirmation and permanently removes all data.</p>"},{"location":"cli/#restore","title":"restore","text":"<p>Restore a soft-deleted DataFrame.</p> <pre><code>dfstore restore &lt;name&gt; [--store-path PATH]\n</code></pre>"},{"location":"cli/#serve","title":"serve","text":"<p>Launch the web UI.</p> <pre><code>dfstore serve [options]\n</code></pre> Option Description <code>--port</code>, <code>-p</code> Port to listen on (default: <code>7860</code>) <code>--host</code> Host to bind to (default: <code>127.0.0.1</code>) <code>--store-path</code> Override store directory <pre><code>dfstore serve\ndfstore serve --port 8080 --host 0.0.0.0\n</code></pre> <p>Requires <code>pip install 'dfstore[gui]'</code>.</p>"},{"location":"configuration/","title":"Configuration","text":""},{"location":"configuration/#store-location","title":"Store Location","text":"<p>By default dfstore saves everything to <code>~/.dfstore</code>. There are three ways to change this.</p>"},{"location":"configuration/#environment-variable","title":"Environment Variable","text":"<pre><code>export DFSTORE_PATH=/data/my_store\n</code></pre> <p>Any Python or CLI call will use this path automatically.</p>"},{"location":"configuration/#per-call-argument","title":"Per-Call Argument","text":"<p>Every API function and CLI command accepts <code>--store-path</code> / <code>store_path=</code>:</p> <pre><code>dfstore.save(df, name=\"data\", store_path=\"/data/my_store\")\ndfstore.get(\"data\", store_path=\"/data/my_store\")\ndfstore.list(store_path=\"/data/my_store\")\n</code></pre> <pre><code>dfstore save data.csv --name data --store-path /data/my_store\ndfstore list --store-path /data/my_store\n</code></pre>"},{"location":"configuration/#dfstore-class","title":"DFStore Class","text":"<p>For scripts that make many calls to the same store, instantiate <code>DFStore</code> directly:</p> <pre><code>from dfstore import DFStore\nfrom pathlib import Path\n\nstore = DFStore(Path(\"/data/my_store\"))\n\nstore.save(df, name=\"employees\")\nstore.get(\"employees\")\nstore.list()\n</code></pre>"},{"location":"configuration/#priority-order","title":"Priority Order","text":"<p>When resolving the store path, dfstore checks in this order:</p> <ol> <li><code>store_path</code> argument passed directly to the function / CLI flag</li> <li><code>DFSTORE_PATH</code> environment variable</li> <li>Default: <code>~/.dfstore</code></li> </ol>"},{"location":"configuration/#store-layout","title":"Store Layout","text":"<pre><code>&lt;store_path&gt;/\n  index.json          \u2190 all metadata (one JSON object per DataFrame)\n  data/\n    &lt;name&gt;/\n      v1.parquet\n      v2.parquet\n      \u2026\n</code></pre> <p>The <code>index.json</code> file is updated atomically on every save. Parquet files are never modified after creation \u2014 each save writes a new file.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install dfstore          # core library + CLI\npip install 'dfstore[gui]'   # + web UI (requires Flask)\n</code></pre> <p>Requires Python 3.11+.</p>"},{"location":"getting-started/#your-first-save","title":"Your First Save","text":"<pre><code>import dfstore\nimport pandas as pd\n\ndf = pd.DataFrame({\n    \"name\": [\"Alice\", \"Bob\", \"Charlie\"],\n    \"age\": [25, 30, 35],\n    \"department\": [\"Engineering\", \"Marketing\", \"Engineering\"],\n})\n\nvr = dfstore.save(df, name=\"employees\", description=\"HR employee roster\", tags=[\"hr\"])\n\nprint(vr.version)   # 1\nprint(vr.shape)     # (3, 3)\n</code></pre> <p><code>save()</code> returns a <code>VersionRecord</code> with metadata about the saved version.</p>"},{"location":"getting-started/#load-it-back","title":"Load It Back","text":"<pre><code>df = dfstore.get(\"employees\")\nprint(df)\n</code></pre>"},{"location":"getting-started/#where-is-the-data-stored","title":"Where Is the Data Stored?","text":"<p>By default, everything is stored in <code>~/.dfstore</code>:</p> <pre><code>~/.dfstore/\n  index.json          \u2190 metadata for all DataFrames\n  data/\n    employees/\n      v1.parquet\n      v2.parquet\n      \u2026\n</code></pre> <p>You can change the location with an environment variable or per-call argument \u2014 see Configuration.</p>"},{"location":"getting-started/#whats-next","title":"What's Next?","text":"<ul> <li>Save &amp; Versioning \u2014 tags, descriptions, notes, polars support</li> <li>Load Data \u2014 version pinning, library selection</li> <li>List &amp; Search \u2014 browse and filter your store</li> <li>Delete &amp; Restore \u2014 soft delete and recovery</li> <li>Web UI \u2014 browser-based interface</li> <li>CLI Reference \u2014 full command reference</li> </ul>"},{"location":"features/delete-restore/","title":"Delete &amp; Restore","text":""},{"location":"features/delete-restore/#soft-delete","title":"Soft Delete","text":"<p>By default, <code>delete()</code> performs a soft delete \u2014 the record is hidden from <code>list()</code> and <code>get()</code>, but all data and metadata are preserved on disk.</p> <pre><code>import dfstore\n\ndfstore.delete(\"old_dataset\")\n</code></pre> <p>The record won't appear in <code>dfstore.list()</code> after this. Attempting <code>dfstore.get(\"old_dataset\")</code> raises <code>DFNotFoundError</code>.</p>"},{"location":"features/delete-restore/#checking-soft-deleted-records","title":"Checking Soft-Deleted Records","text":"<p>Soft-deleted records are still accessible with <code>include_deleted=True</code>:</p> <pre><code>all_records = dfstore.list(include_deleted=True)\n\n# Filter to deleted only\ndeleted = all_records[all_records[\"deleted\"] == True]\nprint(deleted[[\"name\", \"updated_at\"]])\n</code></pre>"},{"location":"features/delete-restore/#restore","title":"Restore","text":"<p>Undo a soft delete:</p> <pre><code>dfstore.restore(\"old_dataset\")\n\n# Now accessible again\ndf = dfstore.get(\"old_dataset\")\n</code></pre>"},{"location":"features/delete-restore/#hard-delete","title":"Hard Delete","text":"<p>Permanently removes all data and metadata. This cannot be undone.</p> <pre><code>dfstore.delete(\"old_dataset\", hard=True)\n</code></pre> <p>After a hard delete:</p> <ul> <li>The Parquet files are removed from disk.</li> <li>The metadata entry is removed from the index.</li> <li><code>restore()</code> will raise <code>DFNotFoundError</code>.</li> </ul>"},{"location":"features/delete-restore/#summary","title":"Summary","text":"Action Effect Reversible? <code>delete(name)</code> Hides the record; data preserved Yes \u2014 <code>restore()</code> <code>delete(name, hard=True)</code> Removes all data and metadata No <code>restore(name)</code> Undoes a soft delete \u2014"},{"location":"features/get/","title":"Load Data","text":""},{"location":"features/get/#basic-load","title":"Basic Load","text":"<pre><code>import dfstore\n\ndf = dfstore.get(\"sales_2024\")\n</code></pre> <p>Returns the latest version as the same library it was saved with (pandas or polars).</p>"},{"location":"features/get/#parameters","title":"Parameters","text":"Parameter Type Description <code>name</code> <code>str</code> Name of the stored DataFrame <code>version</code> <code>int</code> Pin to a specific version (default: latest) <code>as_library</code> <code>\"pandas\"</code> or <code>\"polars\"</code> Force output library <code>store_path</code> <code>Path</code> or <code>str</code> Override the default store location"},{"location":"features/get/#load-a-specific-version","title":"Load a Specific Version","text":"<pre><code>df_v1 = dfstore.get(\"sales_2024\", version=1)\ndf_v2 = dfstore.get(\"sales_2024\", version=2)\n</code></pre> <p>All versions are kept forever (unless hard-deleted), so you can always go back.</p>"},{"location":"features/get/#force-a-library","title":"Force a Library","text":"<p>Load as polars even if it was saved as pandas, and vice versa:</p> <pre><code># Always get pandas\ndf = dfstore.get(\"my_data\", as_library=\"pandas\")\n\n# Always get polars\ndf = dfstore.get(\"my_data\", as_library=\"polars\")\n</code></pre>"},{"location":"features/get/#error-handling","title":"Error Handling","text":"<pre><code>from dfstore.exceptions import DFNotFoundError\n\ntry:\n    df = dfstore.get(\"nonexistent\")\nexcept DFNotFoundError:\n    print(\"DataFrame not found in store\")\n</code></pre> <p><code>DFNotFoundError</code> is raised when:</p> <ul> <li>The name doesn't exist in the store.</li> <li>The name exists but has been hard-deleted.</li> <li>The requested <code>version</code> number doesn't exist.</li> </ul>"},{"location":"features/info/","title":"Inspect Metadata","text":""},{"location":"features/info/#info","title":"info()","text":"<p>Get full metadata for a named DataFrame:</p> <pre><code>import dfstore\n\nr = dfstore.info(\"employees\")\nprint(r)\n</code></pre> <p>Returns a single-row pandas DataFrame:</p> name description tags created_at updated_at current_version deleted employees HR roster hr 2024-01-10 2024-02-01 2 False"},{"location":"features/info/#raw-dfrecord","title":"Raw DFRecord","text":"<pre><code>r = dfstore.info(\"employees\", format=\"raw\")\n\nprint(r.name)             # 'employees'\nprint(r.description)      # 'HR roster'\nprint(r.tags)             # ['hr', {'env': 'production'}]\nprint(r.created_at)       # datetime\nprint(r.current_version)  # 2\nprint(r.deleted)          # False\nprint(len(r.versions))    # 2 \u2014 one VersionRecord per save\n</code></pre>"},{"location":"features/info/#inspecting-versions","title":"Inspecting Versions","text":"<p>The <code>DFRecord</code> includes the full version history:</p> <pre><code>r = dfstore.info(\"employees\", format=\"raw\")\n\nfor v in r.versions:\n    print(f\"v{v.version} \u2014 {v.saved_at:%Y-%m-%d} \u2014 shape {v.shape}\")\n    if v.columns_added:\n        print(f\"  Added: {v.columns_added}\")\n    if v.columns_removed:\n        print(f\"  Removed: {v.columns_removed}\")\n</code></pre>"},{"location":"features/info/#preview","title":"preview()","text":"<p>Load only the first few rows without pulling the full dataset:</p> <pre><code>data = dfstore.preview(\"employees\")         # first 5 rows (default)\ndata = dfstore.preview(\"employees\", n=10)   # first 10 rows\ndata = dfstore.preview(\"employees\", n=3, version=1)  # from v1\n\n# Returns a plain dict \u2014 ready for JSON serialisation\nprint(data[\"columns\"])  # ['name', 'age', 'department']\nprint(data[\"rows\"])     # [['Alice', 25, 'Engineering'], ...]\n</code></pre> <p>This is especially useful when inspecting large datasets or building UIs.</p>"},{"location":"features/list-search/","title":"List &amp; Search","text":""},{"location":"features/list-search/#list-all-dataframes","title":"List All DataFrames","text":"<pre><code>import dfstore\n\ndf = dfstore.list()\nprint(df)\n</code></pre> <p>Returns a pandas DataFrame with one row per stored entry:</p> name description tags created_at updated_at current_version deleted sales_2024 Annual sales finance 2024-01-10 2024-03-05 3 False employees HR roster hr 2024-02-01 2024-02-01 1 False <p>Results are sorted by <code>updated_at</code> descending (most recently updated first).</p>"},{"location":"features/list-search/#include-soft-deleted","title":"Include Soft-Deleted","text":"<pre><code>all_records = dfstore.list(include_deleted=True)\n</code></pre>"},{"location":"features/list-search/#raw-records","title":"Raw Records","text":"<p>To get <code>DFRecord</code> objects instead of a DataFrame:</p> <pre><code>records = dfstore.list(format=\"raw\")\n\nfor r in records:\n    print(r.name, r.current_version, r.tags)\n</code></pre>"},{"location":"features/list-search/#search","title":"Search","text":"<p>Search across all stored DataFrames by description, tags, or column names:</p> By descriptionBy tagBy column nameCombined <pre><code>results = dfstore.search(description=\"sales\")\n</code></pre> <pre><code>results = dfstore.search(tags=[\"finance\"])\n\n# Key-value tag\nresults = dfstore.search(tags=[{\"env\": \"production\"}])\n</code></pre> <pre><code>results = dfstore.search(columns=[\"revenue\", \"region\"])\n</code></pre> <pre><code>results = dfstore.search(\n    description=\"sales\",\n    tags=[\"finance\"],\n    columns=[\"revenue\"],\n)\n</code></pre> <p>All criteria are combined with AND \u2014 a record must match all provided filters.</p>"},{"location":"features/list-search/#search-parameters","title":"Search Parameters","text":"Parameter Type Description <code>description</code> <code>str</code> Substring match against description <code>tags</code> <code>list</code> All specified tags must be present <code>columns</code> <code>list[str]</code> All specified column names must exist <code>store_path</code> <code>Path</code> or <code>str</code> Override the default store location <code>format</code> <code>\"pd\"</code> or <code>\"raw\"</code> Return as DataFrame or list of <code>DFRecord</code>"},{"location":"features/list-search/#version-history","title":"Version History","text":"<p>To see all versions of a specific DataFrame:</p> <pre><code>vrs = dfstore.versions(\"sales_2024\")\nprint(vrs)\n</code></pre> <p>Returns a DataFrame with one row per version:</p> version saved_at notes shape row_diff columns_added columns_removed 1 2024-01-10 Initial (500, 6) 0 [] [] 2 2024-02-15 Add Q1 (620, 6) +120 [] [] 3 2024-03-05 Add region (620, 7) 0 [region] [] <pre><code># Raw VersionRecord objects\nvrs = dfstore.versions(\"sales_2024\", format=\"raw\")\nfor v in vrs:\n    print(f\"v{v.version}: {v.shape}, diff={v.row_diff:+d} rows\")\n</code></pre>"},{"location":"features/save/","title":"Save &amp; Versioning","text":""},{"location":"features/save/#basic-save","title":"Basic Save","text":"<pre><code>import dfstore\nimport pandas as pd\n\ndf = pd.DataFrame({\"a\": [1, 2, 3], \"b\": [4, 5, 6]})\n\ndfstore.save(df, name=\"my_data\")\n</code></pre> <p>Every call to <code>save()</code> with the same name creates a new version. The old data is never overwritten.</p>"},{"location":"features/save/#parameters","title":"Parameters","text":"Parameter Type Description <code>df</code> <code>pd.DataFrame</code> or <code>pl.DataFrame</code> The DataFrame to save <code>name</code> <code>str</code> Unique name (letters, numbers, <code>_</code>, <code>-</code>; max 128 chars) <code>description</code> <code>str</code> Optional human-readable description <code>tags</code> <code>list</code> Optional list of tags (see below) <code>notes</code> <code>str</code> Optional version-specific note <code>store_path</code> <code>Path</code> or <code>str</code> Override the default store location"},{"location":"features/save/#adding-metadata","title":"Adding Metadata","text":"<pre><code>dfstore.save(\n    df,\n    name=\"sales_2024\",\n    description=\"Annual sales data by region\",\n    tags=[\"finance\", \"region=EU\"],\n    notes=\"Includes Q4 revision\",\n)\n</code></pre>"},{"location":"features/save/#tags","title":"Tags","text":"<p>Tags can be plain strings or key-value pairs:</p> <pre><code># Plain string tags\ndfstore.save(df, name=\"ds\", tags=[\"raw\", \"finance\"])\n\n# Key-value dict tags\ndfstore.save(df, name=\"ds\", tags=[{\"env\": \"production\"}, {\"team\": \"data\"}])\n\n# Mixed\ndfstore.save(df, name=\"ds\", tags=[\"finance\", {\"env\": \"production\"}])\n</code></pre>"},{"location":"features/save/#versioning","title":"Versioning","text":"<p>Each save increments the version counter automatically:</p> <pre><code>dfstore.save(df_v1, name=\"dataset\")   # version 1\ndfstore.save(df_v2, name=\"dataset\")   # version 2\ndfstore.save(df_v3, name=\"dataset\")   # version 3\n</code></pre> <p>The <code>VersionRecord</code> returned by <code>save()</code> contains the new version number plus diff information relative to the previous version:</p> <pre><code>vr = dfstore.save(df_v2, name=\"dataset\")\n\nprint(vr.version)          # 2\nprint(vr.shape)            # (100, 5)\nprint(vr.row_diff)         # +20 (relative to v1)\nprint(vr.columns_added)    # ['new_col']\nprint(vr.columns_removed)  # []\n</code></pre>"},{"location":"features/save/#saving-polars-dataframes","title":"Saving Polars DataFrames","text":"<p>dfstore works transparently with polars:</p> <pre><code>import polars as pl\n\ndf = pl.DataFrame({\"x\": [1, 2, 3], \"y\": [\"a\", \"b\", \"c\"]})\n\ndfstore.save(df, name=\"polars_data\", description=\"A polars DataFrame\")\n</code></pre> <p>You can load it back as either pandas or polars \u2014 see Load Data.</p>"},{"location":"features/save/#return-value","title":"Return Value","text":"<p><code>save()</code> returns a <code>VersionRecord</code>:</p> <pre><code>vr = dfstore.save(df, name=\"data\")\n\nvr.version         # int \u2014 version number\nvr.saved_at        # datetime \u2014 UTC timestamp\nvr.shape           # tuple[int, int] \u2014 (rows, cols)\nvr.columns         # list[str]\nvr.dtypes          # dict[str, str]\nvr.null_counts     # dict[str, int]\nvr.notes           # str\nvr.row_diff        # int \u2014 row delta vs previous version (0 for v1)\nvr.columns_added   # list[str]\nvr.columns_removed # list[str]\nvr.library         # 'pandas' or 'polars'\n</code></pre>"},{"location":"features/web-ui/","title":"Web UI","text":"<p>dfstore ships with an optional browser-based interface for exploring and managing your store.</p>"},{"location":"features/web-ui/#installation","title":"Installation","text":"<p>The web UI requires Flask:</p> <pre><code>pip install 'dfstore[gui]'\n</code></pre>"},{"location":"features/web-ui/#starting-the-server","title":"Starting the Server","text":"<pre><code>dfstore serve\n</code></pre> <p>Opens at http://127.0.0.1:7860 by default.</p> <pre><code># Custom host and port\ndfstore serve --port 8080 --host 0.0.0.0\n</code></pre>"},{"location":"features/web-ui/#tabs","title":"Tabs","text":""},{"location":"features/web-ui/#browse","title":"Browse","text":"<p>View all stored DataFrames in a sortable table. Click any row to see:</p> <ul> <li>Full metadata (description, tags, timestamps)</li> <li>Version history with diff information</li> <li>A live data preview of the first few rows</li> </ul>"},{"location":"features/web-ui/#save","title":"Save","text":"<p>Upload a CSV or Parquet file directly from the browser. Fill in the name, description, tags, and optional version notes before saving.</p>"},{"location":"features/web-ui/#search","title":"Search","text":"<p>Filter DataFrames by:</p> <ul> <li>Description substring</li> <li>Tags</li> <li>Column names</li> </ul>"},{"location":"features/web-ui/#manage","title":"Manage","text":"<p>Soft-delete or hard-delete entries, and restore previously soft-deleted ones.</p>"},{"location":"features/web-ui/#screenshot","title":"Screenshot","text":""}]}